# v19 後分析：先修正證據，再決定訓練方向

2026-09-06 America/Los_Angeles。使用者要求先寫下檢討，再一次完成分析；本輪不開 v20，不做新訓練或新老師呼叫。已知 v19 同場結果：新概念 72→96/96、舊技能複核 59→56/96；本分析是事後分析，不能冒稱訓練前預註冊。

## 已同意的四項修正

1. 已知模板新參數的 96/96 不足以證明概念掌握，也不能單憑滿分判定背誦。「真懂通常八十幾分」沒有依據。必須固定權重，再測改寫、條件重排、數字量級及相似題面不同所求量。原題與變體以故事配對，不把相依題當獨立樣本。
2. 舊技能整體 12 題錯變對、15 題對變錯：精確雙尾 McNemar p=0.701108，未偵測到整體差異；不是已證明兩者等效或確定只是雜訊。「恰有一個事件」實際配對為 1 題錯變對、10 題對變錯，p=0.011719，不是 p<0.01。八類皆被檢查，另報 Holm 多重比較；實務保留門檻與統計顯著性分開。不能把其他類一概稱為持平。
3. 固定跨版本基準缺失。原 240 個 MC 回答是 60 題×4 種選項旋轉，統計單位是原題；應同時追蹤總回答正確及四次全對。另固定列式錨點、prompt、grader、生成設定與 hash。舊結果規則不一時不拼湊曲線；缺值留空。新未見測試與已曝光永久錨點分開。
4. 要量訓練 seed 變異，必須重新訓練；同一 checkpoint 換 greedy 推論 seed 不足以估計。重做 v15 需固定其前置 checkpoint，重做 v19 需固定 v15。至少三個訓練 seed 是初步估計，不把三次當精確噪音底線。本輪只記錄後續重複實驗設計，不消耗新訓練。

## 本輪分析與推論探針（在產生新回答前固定）

- 重算 v19 全部八類配對結果、精確 McNemar、Holm 校正與條件於這批題目的不確定性；列出 gains/losses 原文。新概念以 32 個參數故事為群組，不能把 96 個三深度混合題簡單當成全獨立。
- 盤點保存的 MC 腳本與結果，建立永久基準的機器可讀凍結規格；對 v15、v19 用相同固定設定重測 60 題×4 旋轉。這是事後基準重評。
- 概念擾動：從 v19 新概念 test 每個 track×depth 依 ID 取前 2 題，共 24 題（每軌 2 個故事、每故事三深度）。每題三種形式：原題；保持數學關係的改寫／條件重排；數字量級改變（沿用已核對產生器重新計算標準式）。共 72 題，兩個模型 144 回答。只識別這兩類擾動的脆弱性，不宣稱所有形式 OOD 都測過。
- 局部失效探針：舊測試 exactly_one 依 ID 前 8 個參數故事，各問恰有一個與至少一個事件，用一致句式，16 題×2 模型。這是針對已發現錯誤設計的診斷，非全新無偏能力估計。
- 上述推論共 656 回答：MC 480＋概念 144＋事件對照 32。保存原文、參考式、語意複核、配對一致率與所有權重／資料 hash。數學未知數或錯誤概念不能修補；未匹配的等價式逐題複核，不因結果修改模型或再訓練。
- 全部以 Kaggle Save & Run All 正式任務同步執行並保存；GitHub 保存來源、報告及完整資料。執行失敗時保留已得結果並修正執行問題，不能把未完成宣稱完成。

## 預期與可推翻的解釋

若 v19 同參數改寫顯著失分，支持對敘述模板敏感；若量級改變失分，支持數字範圍轉移不足。兩者穩定只能增加有限範圍穩健性證據，不能證明內部採用正確推理。事件對照若至少一個正確、恰有一個錯誤，支持局部事件辨識／公式選擇問題，但仍不能直接宣稱神經網路內部「某個 family 被清除」。

原 v19 的停止與不升級決定維持：它違反事前實務保留門檻，不需要把整體 p 值解讀成顯著退步。本輪只建立更可靠的下一步依據。
