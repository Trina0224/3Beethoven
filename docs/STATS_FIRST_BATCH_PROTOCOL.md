# 第一批學生：執行前記錄

2026-09-07。使用者最新授權：自行完成清理，直接做到第一批學生訓練完成。

動機：教師 C 的 24 個重用開發題，語意複核 22/24，仍有兩個區間縮放真錯誤。不能把格式失敗當數學錯誤，也不能把修正後開發題分數當學生能力。

方法：保留現有 96 故事、268 題候選與所有凍結驗證／測試；重用 V49 原始回答中重新驗證合格的內容。只補缺失教材。教師新提示改為簡短 answers JSON，提供通用符號規則（含區間半寬除以樣本倍率平方根），不提供題目數值答案或參考式。新資料標註 rule_assisted；此為規則輔助 response distillation，不宣稱 unaided teacher。學生看不到教師規則提示。A/B/C 開發題不加入教材。

預期：避免區間方向錯誤、Poisson 單位遺漏和過長輸出，取得逐類合格的教材；混入固定 192 筆 replay 後，兩個 seed 的舊／組合能力保留率會比事件單獨強化穩定。若仍有逐類保留失敗，這項預期不成立，保留診斷權重、不升級。

第一批先完成原 v15 接續 LoRA 的 seed 2027、31415；fresh base 對照仍屬原四次比較的後半批，不能用此兩次報告宣稱已完成起點比較。每次最多一遍、49 updates，12、24、最後一步驗證保存，首個全面通過即停。不啟動第二輪，不用獨立測試選點。原逐類 retention 門檻保持不變。沒有通過也保存最後診斷 adapter，區分訓練完成與升級成功。

教師 gate 不變：pilot 至少 59/65 且每 archetype 至少 80%；full 每 target category 至少 80%，無未決評分。輸出以獨立 Fraction oracle 和已審核結構驗證；新增允許已在 C 審核的部分常數折疊，絕不改錯誤表達式的數值。歷史原始回答、原分數、C 程式保留。

預算：既有 110 calls / US$0.00806302。優先重用，正常預期新增約 77 calls；此次新呼叫硬上限 192（每故事最多兩次、重試有提醒），另保留 24 次 benchmark。因此把先前工程性總呼叫上限 216 明確調整為 326；金額硬上限仍 US$3，含既有成本，缺 cost 或不明網路結果不自動重呼。此調整在新呼叫前記錄，依使用者現行完成授權與先前 quota 充足說明，不是放寬資料驗收。

限制：候選仍為受控題族，不是未知概念測試；兩個 seed 不足以精確估計變異。此批不新增完整全域 1→2→3 課程排程，保留故事內順序與 replay 的既定對照，避免同時改更多變因。

## 執行中修訂（8 次新呼叫後、學生訓練前）

新呼叫 US$0.00031254，pilot 已接受 62/65。兩個區間題仍把端點當中心，一個 Poisson 二階動差留下 Var(X) 或錯誤換算。這是具體真錯誤，原回答不修值、不放行。把區間提示集中成一般 L/U/k 公式，Poisson 提醒 m 必須完整代入兩處；仍不提供數值參考答案。每故事最多三次（總新呼叫 192 與 US$3 上限不變），第三次是針對已觀察錯法的新提示，不是重送同一請求。其他題族不改提示。此修訂先提交再呼叫；pilot gate 仍須通過才往 full 推進。

## 區間輸出格式修訂（第三次針對性補題後）

Poisson 已補齊。區間 000 的新式數學正確但 JSON 缺結尾引號，001 仍多乘一個 0.5。保留兩筆原文，不代改答案。區間改用單題 `Expression:` 輸出，消除不必要的 JSON 外殼；只對區間允許第四次呼叫，其他題族最多三次，總預算不變。模型原文字串和對應題號存檔，固定唯一題目才能採用單行傳輸。此為格式／教學調整，不是新的概念測試。另將 pilot 驗收的重複資料重建改成一次查表，計分規則不變。
