## 執行前修復定案：較小更新幅度

2026-09-07 PDT，尚無修復 updates。已見原 v15 與 step24 的 224/224 選點原答完全重現，兩者三類 replay 皆 72/72；step33 控制仍在進行。採條件式開跑：必須等 CONTROL_COMPLETE，且三個權重 336/336 選點原答全相同，才啟動修復。

唯一優化變因是 LR 2e-5 → 5e-6。仍從同一原 v15 開始，原樣 67 新＋192 replay、相同 seed 2027／31415 和各自排序、一遍33步上限，12／24／33驗證，首次全門檻通過即停。max_updates 的欄位49改33只是將本批既有實際一遍上限寫明，不增減可用資料或原本33步曝光。零新增教師呼叫、不改 loss function／rank／dropout／grader／門檻。

預期較小更新可減少已能作答表述的漂移，同時仍學到事件區別；若舊能力仍明顯崩落或新事件完全無改善，則削弱這個修復假設。此試驗不聲稱已定位單一根因，也不聲稱單靠較小 LR 能建立概念泛化。

新增觀測不影響優化：記錄每一實際 microbatch 的 input/label SHA 與 supervised token 數；核對原題目和原答 SHA 後，重用 Version53 已定案的語義複核，未知答案仍暫停待判。保存原自動分與複核來源。原選點矩陣與逐類條件完全不改。獨立測試只評估通過選點的候選；若沒有通過，保存失敗修復結果，不加第三個 seed、不延長訓練。
