# Compact teacher output — preregistered continuation

2026-09-07. Authorized by the user's continuation after the completed A/B report. Commit before new calls. Prior observed results: A auto 9/24, reviewed 11/24; B auto 0/24, diagnostic 12/24, 11 truncated outputs. These exposed development questions are reused deliberately for a paired engineering check, not a new unseen benchmark.

Hypothesis: explicit compact-only output instructions remove prose-induced truncation without changing the 400-token cap. This may improve usable responses; mathematical errors can remain. The intervention includes conservative envelope extraction and previously verified coefficient-folding identities, so distinguish prompt improvement from parser/grader gains by re-scoring old B under the exact new contract at zero cost.

- Same 24 development questions, three families of eight; same model, fixed DeepInfra provider, no fallback, temperature 0, JSON-object request, max_tokens 400. One new request per question, no retries and no correct values supplied. Name the same intermediate quantities as B, request only compact JSON with numerical-expression strings and no commentary or Markdown.
- New acceptance contract: exact one complete JSON object, or exactly one complete fenced JSON block with no competing JSON/answer outside it. Never repair truncation or expressions. Preserve raw and extraction metadata. Require the exact expected answer ID and exact intermediate keys. All intermediates must match independent rational oracles; final expression must match the frozen structural grader or a preregistered partially folded affine identity. Final-number-only credit remains forbidden.
- Final usable gate remains >=22/24 total and >=7/8 in each family; no unresolved semantic reviews or charges. Separately report strict JSON count, accepted with extraction, truncations, true math errors, paired old-B/new-C flips under this common contract. Do not retrospectively overwrite A/B files or old gates.
- New calls <=24. Prior completed calls 86 and cost $0.00693694; count both prior runs. Keep aggregate $0.50 screening ceiling, $3 global ceiling, 216 global call limit including final benchmark, and $0.01 next-call reservation. Missing/invalid charges or unresolved attempts stop further calls. No reference-assisted generation, model switch or token-cap increase in this round.
- If the screen passes, prepare the remaining corpus within the existing global budget and original family/retention gates. Passing this reused development set is only an engineering gate, not evidence of generalization. Full-generation grouping, wording revisions and request hashes must be frozen before generation; the 288 held-out questions and benchmark cannot be used to choose prompts. Student training remains conditional on the corpus gate and existing two starts x two seeds, one pass protocol.
- If it fails, finish the 24-call screen, resolve diagnostics and save a concrete stop result. Do not silently escalate output tokens, teach from references, or train on failed rows.

The original 400-token cap is intentionally unchanged. If output is still truncated, that directly weakens the compact-prompt hypothesis and is evidence for a separately budgeted contract change, not permission for repeated ad hoc prompting.
