# 第一批小批學生訓練：執行前範圍修訂

2026-09-07，尚無新學生 updates。使用者授權自行清理、直接完成第一批學生。

完整候選方案的工程驗收仍未通過：原 pilot 64/65，擴充 pilot 68/71，區間 5/8。原結果與失敗 gate 保留；不把這些數字改稱完整教材合格。停止更多教師提示／重呼，不讓同一個教材工程問題再次無限延後学生實驗。

本次第一批改成 **filtered pilot student 診斷訓練**：只使用目前逐題確認正確且可執行的新增教師列式（預計 train 67、validation 1），與固定 192 筆舊 replay 混訓。開跑必須重新驗證每筆原始回答、題目雜湊與 target；至少 60 個新 train rows，十個 archetype 均有教材。三筆拒收／未決題不進學生資料，候選 validation 不進 train。這是改變資料量和 replay 占比，不能冒充原 198 新教材的完整比較。

兩次均從同一原 v15 adapter 起跑，seed 2027、31415，LR 2e-5，一遍上限，實際步數 ceil(rows/8)，12／24／最後一步驗證並存檔，首次全 family 通過即停；不進第二輪。112 題選點矩陣與逐類保留門檻完全不變；獨立 holdout 不用於選點。即使 gate 未通過，也保存最後診斷 adapter，不補種子／延長訓練。

預期：在較高 replay 比例下，確認混合概念教材能否改善事件能力並保留原技能；若任一 family 持續掉分，記錄它，不能以總分掩蓋。此批只提供初步方向與可攜權重，**不具升級資格**，也不能回答 fresh base 和 v15 何者較好。完整四次起點比較仍未完成。

舊 replay 192 筆已重驗算，沒有數值錯誤；五筆 binomial 為把 n-r 算成整數與約分後的標準二項式，人工判定正確，原 target 不改。所有新入選資料仍必須通過原有自動數學與結構檢查。

此調整在新學生訓練前明確另立 protocol 與 decision；原 full teacher gate 不解鎖、不覆寫。完成判準為兩個 seed 都有 training_complete、驗證歷程、有限數值的 adapter 與 SHA-256、可下载保存快照；完成與改善是兩回事。
