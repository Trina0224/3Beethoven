# v0.20：一輪上限與事件對照教材（執行前規則）

2026-09-06 PDT。使用者授權執行「1＋3」，本輪是 v19 seed replication 的延伸。尚未有本輪模型結果。

動機：原 v15 在歷史 exactly_one 有 10/12，但事件改寫不穩；v19 三個 seed 此類皆下降。兩個 seed 第二輪另有 scaling variance 局部損失。因此先改事件教學並限制訓練長度，不混入新的 scaling variance 句式。

預期：同故事對照恰好一個、兩者皆有、兩者皆無、兩者相同，有助分辨事件集合；若僅原句式改善而改寫未改善，則不支持語意遷移。這是整個教學配方試驗，未設等量重複教材對照，不能獨立歸因對照排程或 replay 比例。

- 固定原 v15 父模型 SHA-256：9369d52de4a886df9da0c872cd41bd4e01af0a38bf02ad724b5951c1a6b9f5d3。
- seeds 2027、31415，兩次均從上述父模型開始，不接續彼此。
- 新程序化教材：24 組機率參數，每組四事件、兩種說法，共 192 筆；加原 v19 八類各24筆教師 replay，共384筆。新增答案是程序化 SFT，不稱為新教師蒸餾。
- 四種事件同故事相鄰，四筆新題接四筆 replay；每個 seed 固定打亂故事組與 replay 的順序，跨組數學參數保持分割。每8筆一次更新。
- 兩事件明確獨立；機率以101為分母，非0或1、兩機率不同且和不為1。訓練／驗證／測試 unordered 參數組不重疊。這是內部生成保留題，不是未知概念測試；測試新說法仍由同一研究流程設計。
- 驗證8組×4事件×2說法=64題；新測試24組×4事件=96題，使用未出現在訓練的 indicator/agreement 說法。測試前凍結完整題目、來源與 hash。
- 最多一輪48updates，LR2e-5 constant，effective batch8，沿用既有4-bit QLoRA。24、48步保存權重；不啟動第二輪，也不因結果不佳增加更新。
- 所有權重以相同直接 PeftModel 推論載入方式驗證，避免訓練中 dtype 與推論不同造成選點偏差。先完成兩個checkpoint驗證再揭示測試。
- 保留驗證沿用 v19 old_validation：總分至少父模型−2，每類至少父模型−1。通過者按新事件驗證正確數選點，同分取較早；無通過者維持原v15，最後checkpoint僅作診斷。
- 同場評估父模型與兩個候選：新事件96、永久舊列式96、新組合96、永久MC240。每題保留raw、hash、自動分與待複核標記。已曝光永久題是回歸檢查，不稱新盲測。
- 探索性升級條件：兩個seed皆通過驗證保留門檻；新測試 exactly_one 比父模型至少多對4/24；四事件合計至少多對8/96；舊列式總分最多失4/96、每類最多失2/12；MC最多失4/240。否則不升級，逐類報告。題目同故事相關，不將96題當96獨立抽樣；兩seed只作初步複驗。
- 新教師呼叫0。完成每個seed即保存權重及回答，產生ZIP與SHA-256；GitHub保存程式、題集、規則與摘要，Kaggle保存二進位權重。

## 執行前掛載修正

Version 44 在父模型存在檢查處停止，零訓練更新。已從 scriptVersionId=347852579 的 Output 頁確認 Version 42 含原 v15 目錄；改用官方 kagglehub 明確指定 `trinashih/3beethoven-v0-2/versions/42`，傳入回傳路徑並保留原 SHA-256 強制核對。這只修正輸入取得方式，不改教材、種子或配方。
